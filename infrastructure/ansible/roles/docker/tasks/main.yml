---
- name: Install Docker
  ansible.builtin.apt:
    name: [docker.io, docker-compose-v2]
    state: present
    update_cache: yes
  become: true

- name: Add vagrant to docker group
  ansible.builtin.user:
    name: vagrant
    groups: docker
    append: yes
